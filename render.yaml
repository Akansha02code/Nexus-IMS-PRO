services:
  - type: web
    name: nexus-ims-backend
    env: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -Xmx256m -Xms128m -jar ims-backend/target/ims-0.0.1-SNAPSHOT.jar --spring.profiles.active=render
    healthCheckPath: /api/health
    healthCheckInterval: 30
    healthCheckTimeout: 5
    healthCheckUnhealthyThreshold: 3
    envVars:
      - key: JDBC_DATABASE_URL
        fromDatabase:
          name: nexus-ims-db
          property: connectionString
      - key: DB_USER
        value: postgres
      - key: JWT_SECRET
        value: 9d\RT%(`uLQH-s'G`V-<\+d"i)y?-nvb/??&Zs38T"miCt_Zo/Kb)@R^e*^iLyMk
      - key: CORS_ORIGINS
        value: https://your-frontend.onrender.com,http://localhost:5173
      - key: SPRING_PROFILES_ACTIVE
        value: render

databases:
  - name: nexus-ims-db
    databaseName: nexus_ims_db
    username: postgres
    plan: free
    region: oregon
